#include <iostream>

using namespace std;

int main()
{
	setlocale(LC_ALL, "ru");

	cout << "Задача 1." << endl << "Сервер выходит из строя из-за перегрева, когда кондиционирование воздуха выключено."s << endl;
	cout << "Он также выходит из строя из - за перегрева, если барахлит кулер.При каких условиях сервер работает? "s << endl << endl;

	cout << "Ответьте на вопросы при помощи \"1\"(да) или \"0\"(нет)."s << endl << endl;

	string D = "Сервер вышел из строя."s;
	int B, C;

	cout << "Кондиционирование отключено?"s << endl;
	cin >> B;

	cout << "Кулер барахлит?"s << endl;
	cin >> C;

	if (B == 1 || C == 1)
	{
		cout << D << endl;
	}
	else
	{
		cout << "Сервер продолжает работать."s;
	}

	return 0;
}